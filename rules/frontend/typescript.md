+++
id = "frontend.typescript"
title = "TypeScript"
severity = "preferred"
scopes = ["frontend"]
+++
# TypeScript

- For greenfield frontend work, prefer TypeScript with strict typing.
- Preserve an existing JavaScript/other frontend stack unless the user approves migration.
- Keep API/domain types explicit and avoid `any` as a default escape hatch.

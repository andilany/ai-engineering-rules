+++
id = "infrastructure.prometheus-grafana"
title = "Prometheus and Grafana"
severity = "conditional"
scopes = ["infrastructure"]
+++
# Prometheus / Grafana

- Prefer Prometheus-compatible metrics and Grafana dashboards when the existing monitoring stack supports them.
- Measure latency, errors, saturation, queue depth, retry/failure rates, and domain SLIs relevant to the service.

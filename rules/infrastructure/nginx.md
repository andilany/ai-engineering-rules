+++
id = "infrastructure.nginx"
title = "Nginx"
severity = "conditional"
scopes = ["infrastructure"]
+++
# Nginx

- Use Nginx when the existing project already relies on it or its feature/performance/configuration needs justify it.
- Do not replace working Nginx with Caddy without explicit user approval.

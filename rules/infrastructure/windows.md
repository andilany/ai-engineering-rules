+++
id = "infrastructure.windows"
title = "Windows"
severity = "conditional"
scopes = ["infrastructure"]
+++
# Windows

- Support Windows development/runtime only where project requirements demand it; avoid assuming GNU-only shell behavior in cross-platform tooling.
- Keep Windows-specific scripts/config isolated and documented.

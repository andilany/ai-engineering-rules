+++
id = "security.rbac"
title = "RBAC"
severity = "conditional"
scopes = ["security"]
+++
# RBAC

- Enforce RBAC server-side at the appropriate boundary and deny access not explicitly granted.
- Keep roles/permissions centrally defined and test both allowed and forbidden paths.
- UI hiding is not authorization.

+++
id = "data.mongodb"
title = "MongoDB"
severity = "optional"
scopes = ["data"]
+++
# MongoDB

- Use MongoDB when the domain and query/access patterns genuinely benefit from document storage.
- Do not introduce MongoDB next to PostgreSQL without a concrete ownership boundary and operational reason.
- Preserve an existing MongoDB design unless the user approves migration.

# Private distribution note

This repository is intended to remain private unless the owner explicitly chooses a public license and distribution model later. The current distribution terms are defined in the root `LICENSE` file (`LicenseRef-Proprietary`).

The rule-pack was independently authored from engineering requirements and reviewed ideas. It does not copy the text of the unlicensed `coderroleggg/rules` repository. Concepts that were useful were re-designed as project-aware invariants, conditional preferences, and user-decision boundaries.

Do not publish private repository URLs, credentials, tokens, or environment-specific paths in generated documentation or releases.
